/*
 * memcopy fallback — only compiled when both ASM paths define it.
 * The actual fallback is in match_fallback.c alongside cp_memcopy.
 * This file is intentionally empty — kept for CMakeLists symmetry.
 */
